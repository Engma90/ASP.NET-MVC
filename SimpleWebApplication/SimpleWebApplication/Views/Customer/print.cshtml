@model SimpleWebApplication.Models.Customer

@{
    ViewBag.Title = "print";
}

<h2>print</h2>
@using (Html.BeginForm("print", "Customer", FormMethod.Post))
{
    <input id="txtname" type="text" name="name" />
    <input id="Submit1" type="submit" value="submit" />
}
@if (ViewBag.result != null)
{
    <h1 class="alert alert-info">@ViewBag.result</h1>
}

<div>
    <h4>Customer</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

    </dl>
</div>

